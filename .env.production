# Loaded automatically during `vite build` (production).
# Safe to commit — URLs only. Do not put VITE_API_KEY here.
# Set VITE_API_KEY as a CI/hosting secret at build time.

VITE_API_BASE_URL=https://spadaptor-api.clashdx.com
VITE_WS_URL=https://spadaptor-api.clashdx.com/v1/stream
